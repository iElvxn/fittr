---
topic: Fittr Phase 1: wardrobe and outfit loop MVP (accounts, wardrobe, fits, planner, share, sync; no in-app social)
updated: 2026-09-10T23:56
---

- (note) sources: docs/phase-1-spec.md (primary, already-distilled Phase 1 spec) and Fittr MVP Requirements.md (ancillary background, full MVP incl. deferred social layer)
- (note) topic scoped to Phase 1 only: wardrobe + outfit loop with accounts and cloud-backed sync, no in-app social; Share = export to iOS share sheet
- (capability) CAP-1 Accounts — intent: user can create and manage an account via Sign in with Apple or email/password, sign out, delete account, with required display name and optional avatar. success: sign up, sign in, sign out, and delete account all complete on a dev build
- (capability) CAP-2 Wardrobe — intent: user can add clothing items from camera or library with on-device background removal, category, auto-detected editable color, and optional name/brand/notes, then view in a filterable grid, edit, or delete any item. success: ten items added from photos in under three minutes by either capture method, background removal acceptable on typical clothing photos laid flat or on a hanger
- (capability) CAP-3 Batch add — intent: user can select multiple library photos at once, or capture several in rapid succession without leaving the camera, and review every item in one shared queue before saving. success: library multi-select and rapid-camera batches both land in the same queue/review screen, each item confirmed individually before save
- (capability) CAP-4 Fits — intent: user can start a Fit from a template that seeds initial item placement, add wardrobe items from any category onto a freeform canvas, freely drag/pinch/rotate/reorder them, then name, save, edit, delete, or favorite the Fit. success: a Fit built from wardrobe items across categories can be freely rearranged, saved, edited, and exported as a share image
- (capability) CAP-5 My Fits — intent: user can browse saved Fits in a grid filterable to All, Favorites, or Worn. success: the My Fits list correctly narrows to just favorited or just worn Fits when filtered
- (capability) CAP-6 Planner — intent: user can assign a saved Fit to a day on a weekly calendar, view/replace/remove that assignment, and see today's planned Fit on Home. success: a week can be fully planned and a Fit marked worn directly from Home
- (capability) CAP-7 Share — intent: user can export a Fit's rendered collage image to the iOS share sheet. success: share produces a correct collage image the iOS share sheet can send onward
- (capability) CAP-8 Sync — intent: the app reads and writes SQLite locally first and reconciles with Postgres in the background, so every wardrobe, Fit, and planner action works offline. success: deleting and reinstalling the app restores all wardrobe, Fit, and planner data from Postgres
- (capability) CAP-9 Privacy — intent: all wardrobe, Fit, and account data stays private to its owner, and deleting an item, Fit, or the account removes the corresponding rows and stored images. success: row-level security rejects cross-user access, and account deletion hard-deletes all of a user's rows and storage objects, then the auth user
- (capability) CAP-10 Measurement — intent: the app emits funnel analytics events and crash reports from first launch so usage and stability are measurable throughout Phase 1. success: a test event reaches PostHog on a dev build and the full signup-through-return funnel can be reconstructed from recorded events
- (constraint) Minimum iOS 17.0, required by the on-device background-removal (subject-lifting) module — rules out supporting older iOS versions
- (constraint) Solo developer on Windows: all native builds run on EAS; development uses an EAS development build on a physical iPhone since Expo Go can't host the background-removal module
- (constraint) Local-first architecture: every read and write targets SQLite; Postgres sync runs only in the background and never blocks the UI
- (constraint) Sync conflicts resolve last-write-wins on updated_at, accepted for single-user/rare-multi-device use; documented as a deliberate simplification, revisit if multi-device use grows
- (constraint) Background removal runs on-device rather than via an API, for cost, privacy, and speed
- (constraint) Category selection is manual in Phase 1 — no AI-based category detection (deferred until it can sit behind a server function)
- (constraint) No Redis, queues, or streaming in Phase 1
- (constraint) Supabase Storage sits behind an ImageStore interface so the storage backend can be swapped later (e.g. S3, R2)
- (constraint) The original photo is discarded once its cutout is confirmed and is never uploaded or retained; a poor cutout is fixed by re-photographing, not reprocessing
- (constraint) No data-export feature in Phase 1 — sync-restore-after-reinstall and account deletion already cover the restore/privacy needs export would have served
- (decision) Non-goal: in-app social entirely — no posts, feed, likes, comments, follows, saves, discover, or notifications; Share means exporting an image to the iOS share sheet only
- (decision) Non-goal: usernames are collected at onboarding but not surfaced in Phase 1 UI beyond profile settings; no username-based search
- (decision) Non-goal: AI-based clothing category detection
- (decision) Non-goal: outfit recommendations or an AI stylist
- (decision) Non-goal: weather-aware outfit recommendations
- (decision) Non-goal: commerce, affiliate shopping, or marketplace features
- (decision) Non-goal: Android and web platforms
- (decision) Success signal: Phase 1 validates 'people will repeatedly use a digital version of their wardrobe to create, save, plan, and share outfits' when five test users can explain what Fittr is, why to add clothes, how to make a Fit, and why to come back, and a TestFlight cohort's two-week funnel (signup to 5 items to first Fit to second Fit to worn/planned to returned the following week) shows repeated use past initial wardrobe setup
- (question) Which additional Fit templates beyond 'Top + Bottom + Shoes', 'Layered Outerwear', and 'Blank canvas' should ship (CAP-4) — finalize once the canvas is built and a few Fits have been made by hand
- (note) companion stack.md created: Expo/RN/TypeScript platform and stack table (app, data, auth, images, state, canvas, styling, testing, CI, observability)
- (note) companion data-model.md created: profiles/wardrobe_items/fits/fit_items/fit_wears/planned_fits tables, referential rules, row-level security, storage layout
- (note) companion sync-design.md created: local-first push/pull mechanics, sync triggers, conflict policy, failure handling
- (note) companion image-pipeline.md created: capture-to-cutout pipeline steps and rapid camera mode
- (note) companion screens-and-flows.md created: navigation and per-screen behavior for auth/onboarding, Home, Wardrobe, Fit builder, My Fits, Planner, Profile, plus list states
- (note) companion analytics-and-metrics.md created: Phase 1 event table, activation funnel, and the MVP's north-star/activation/engagement/retention metric definitions carried forward from the MVP requirements
- (note) companion milestones.md created: M0-M6 milestone table with deliverables and done-when bars
- (note) companion product-context.md created: problems solved, target users, jobs-to-be-done, and product principle carried forward from Fittr MVP Requirements.md for downstream Phase 2/PRD use
- (constraint) No custom backend or serverless functions beyond Supabase, except where a secret or trust boundary requires one (caught in preservation pass)
- (constraint) Projected Supabase free-tier storage usage must stay within limits for the launch cohort (M6), a launch gate not an aspiration (caught in preservation pass)
- (decision) Referential rule clarified in data-model.md: a Fit that still has other items after one is deleted keeps them and shows a visible gap at that canvas position, rather than being deleted itself (caught in preservation pass)
- (event) self-validate pass 1 (coherence, rules 1-6,8): PASS - all 10 capabilities carry intent+success, IDs stable/unique, constraints each rule something out, 7 non-goals present, success signal concrete, prose lean
- (event) self-validate pass 2 (preservation): walked docs/phase-1-spec.md and Fittr MVP Requirements.md claim by claim; found and fixed 3 gaps (no-custom-backend constraint, free-tier storage gate, partial-item-removal gap rule); no wrapper-only drops; verdict PASS after fix
- (decision) Reversed local-first architecture: Phase 1 now reads/writes Supabase Postgres directly, no SQLite/local-first storage; offline support deferred to a later phase (user: 'maybe we can do sqlite later, lets just stick with supabase', confirmed online-only for Phase 1)
- (constraint) The app requires network connectivity for all reads and writes in Phase 1; no offline mode (supersedes the prior local-first constraint)
- (decision) Non-goal added: offline usage / local-first storage, deferred to a later phase
- (capability) CAP-8 redefined from Sync to Cloud persistence — intent: the app reads and writes directly to Supabase Postgres and Storage over the network; no local database or background sync engine. success: wardrobe, Fit, and planner data created or edited on one device are immediately visible after a reinstall or on another signed-in device, since all data lives in Supabase
- (question) How should adding an item or saving a Fit behave when the network is unavailable, now that there's no local-first fallback — block-and-retry the save, or queue just the pending image upload? Needs a decision before implementation (affects CAP-2/CAP-4/CAP-8)
- (note) stack.md updated: removed expo-sqlite/Drizzle local-data row; State row now reads/writes directly against Supabase via TanStack Query
- (note) data-model.md updated: schema lives in Postgres only, no SQLite mirror; dropped dirty/synced_at rationale for client-generated ids
- (note) image-pipeline.md updated: cutout/thumbnail upload and item-row insert happen directly against Supabase, not deferred to a background sync step
- (note) milestones.md M4 updated: direct Postgres/Storage wiring replaces the local-first push/pull sync engine
- (note) sync-design.md unlisted from companions (kept on disk, unused): describes the local-first push/pull design, now deferred until offline support is prioritized
- (event) self-validate re-run after online-only pivot: coherence pass PASS (CAP-8 still has intent+success, ID unchanged; new constraint rules out offline; new non-goal explicit); preservation pass PASS (no source claims lost, sync-design content preserved on disk for future reference, not silently deleted)
- (capability) CAP-11 Wear streak — intent: user can see their current consecutive-day streak of marking a Fit worn, computed passively from existing fit_wears data. success: the displayed streak count increments when today has a wear entry continuing yesterday's chain, and resets to zero after a day with no wear entry
- (decision) Streak requires a saved Fit per day (reuses FR25/fit_wears 'Wear today' mechanism); no new table, no raw check-in without a Fit. No push notifications or reminders for the streak — stays consistent with the existing no-notifications non-goal (surfaced during epics-and-stories Epic 4 review, user requested a daily fit streak feature)
- (event) spec update: added CAP-11 (Wear streak) after epics-and-stories review surfaced the request; self-validate re-run: coherence PASS (intent+success present, new ID CAP-11 doesn't collide), preservation PASS (no source content lost, purely additive)
- (decision) Resolved the network-unavailable save-behavior open question (CAP-2/CAP-4/CAP-8): block-and-keep — on save failure, show 'No connection — nothing was lost. Try again.', keep the user on the unsaved screen with input intact, offer retry; no silent background queue (there is no local-first layer to queue against safely in Phase 1). Decided during bmad-ux Finalize and recorded in EXPERIENCE.md's State Patterns; logged back here per that doc's Open Items note.
- (note) SPEC.md re-derived: added no-connection save-behavior constraint (block-and-keep, citing EXPERIENCE.md), removed the now-resolved Open Question, registered DESIGN.md and EXPERIENCE.md as adopted companions in frontmatter (per SPEC Law rule 7 — the UX contract is now load-bearing for CAP-2/CAP-4/CAP-8 stories).
- (event) self-validate re-run after logging block-and-keep resolution: coherence PASS (constraint rules out silent-queue/spinner behavior, non-goals/success signal unchanged); preservation PASS (sync-design.md's local-first content still on disk, unlisted, untouched; no source claims lost)
- (capability) CAP-1 Accounts expanded: added Google Sign-In as a third account-creation/sign-in method alongside Sign in with Apple and email/password (user request during Story 1.1 build planning). intent/success unchanged in substance, just broadened to name all three methods.
- (constraint) Security baseline for auth (raised during Story 1.1 planning, applies project-wide): session tokens stored via platform secure storage (e.g. expo-secure-store), not plain AsyncStorage; RLS enabled from the very first migration, never added later; no API keys/secrets embedded in the client bundle; all Supabase traffic over HTTPS; OAuth/Apple identity tokens verified server-side through Supabase's own provider flow, not a custom trust boundary.
- (decision) Design exception for Google Sign-In: Google's brand guidelines require its official multi-color G logomark on the sign-in button, which cannot be recolored. DESIGN.md's pure-monochrome rule gets one narrow exception for that logomark only, same pattern as the existing destructive-red exception; button chrome and text stay monochrome. Follow-up needed in DESIGN.md's Colors/Components sections (bmad-ux territory, not bmad-spec's to edit directly).
- (event) self-validate re-run after Google Sign-In + security-baseline + design-exception additions: coherence PASS (CAP-1 intent+success still paired, ID unchanged; new constraints each rule something out); preservation PASS (no source claims lost)
- (note) stack.md updated: Auth row corrected to include Google alongside Apple/email (was missed when CAP-1 was updated earlier); added signInWithIdToken and secure-storage detail. Caught by an independent spec-review subagent during Story 1.1 planning.
- (decision) data-model.md fixed per independent spec review: (1) profiles.username/display_name sequencing clarified — both NOT NULL but system-generated/placeholder at row creation (sign-up), display_name overwritten by onboarding; (2) RLS section rewritten from one blanket 'id/user_id = auth.uid()' rule to explicit per-operation USING/WITH CHECK policies, and profiles gets no client-side DELETE policy (account deletion is service-role, Epic 6).
- (event) self-validate re-run after stack.md/data-model.md corrections from independent Story 1.1 spec review: coherence PASS, preservation PASS (no source claims lost, purely clarifying fixes)
- (constraint) Corrected via live Supabase docs check (user request): session storage baseline was overstated as 'expo-secure-store, never AsyncStorage' -- expo-secure-store has a ~2048-byte per-item limit a full Supabase session can exceed. Actual pattern (Supabase's own quickstart): encrypted session blob in AsyncStorage, AES key held in expo-secure-store. Constraint restated as 'never *unencrypted* AsyncStorage', not 'never AsyncStorage' at all.
- (note) Google Sign-In dashboard config detail added (from Supabase's dedicated Google auth guide, more specific than the general quickstart): Supabase dashboard needs BOTH Web Client ID and iOS Client ID in the Google provider's Client IDs field (comma-separated), plus 'Skip nonce check' enabled for iOS. Confirms the native signInWithIdToken approach (not browser-based signInWithOAuth) is correct for this native iOS app.
- (event) self-validate re-run after docs-verified storage-adapter and Google-dashboard corrections: coherence PASS, preservation PASS
