# BMad skill installs (reinstallable via `npx skills add` + skills-lock.json)
.claude/skills/
.agents/
agent/

# BMad runtime (reinstallable via `bmad setup`; render/ is a transient per-run cache)
_bmad/render/
_bmad/method/
_bmad/scripts/
_bmad/toolbox/

# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native (generated; not committed since this app has no custom native code)
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
/ios
/android

# Metro
.metro-health-check*

# Testing
coverage/

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# Env files — never commit real credentials
.env
.env.local
.env*.local

# TypeScript
*.tsbuildinfo

# EAS
.eas/
